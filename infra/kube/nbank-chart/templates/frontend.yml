apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: nobugsme/nbank-ui:with_nginx
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: frontend
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort

#  ┌─────────────────────────────────────────────┐
#  │                  [User]                     │
#  │    Browser → http://localhost:3000          │
#  └─────────────────────────────────────────────┘
#  │
#  ▼
#  (kubectl port-forward svc/frontend 3000:80)
#  │
#  ▼
#  ┌─────────────────────────────────────────────┐
#  │           [Kubernetes Service]              │
#  │   name: frontend                            │
#  │   type: NodePort (not used here)      │
#  │   port: 80                                  │
#  │   targetPort: 80                            │
#  └─────────────────────────────────────────────┘
#  │
#  ▼
#  ┌─────────────────────────────────────────────┐
#  │                [Pod]                        │
#  │   label: app=frontend                       │
#  │   created via Deployment                    │
#  └─────────────────────────────────────────────┘
#  │
#  ▼
#  ┌─────────────────────────────────────────────┐
#  │         [Container in Pod]                  │
#  │   image: nobugsme/nbank-ui:with_nginx       │
#  │   containerPort: 80                         │
#  └─────────────────────────────────────────────┘
